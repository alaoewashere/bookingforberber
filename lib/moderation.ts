export { normalizeCustomerName, normalizeForModeration, compactModerationKey, validateNameShape, validateNameField, validatePhone, validateEmail, validateService } from "@/lib/moderation/normalize";
export type { ModerationResult } from "@/lib/moderation/server";
